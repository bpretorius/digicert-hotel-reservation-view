{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start React App and Open Safari",
            "type": "node-terminal",
            "request": "launch",
            "command": "BROWSER='open -a Safari' npm start",
            "cwd": "${workspaceFolder}"
        }
    ]
}